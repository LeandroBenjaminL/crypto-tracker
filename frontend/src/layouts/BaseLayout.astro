---
import ThemeToggle from "../components/ThemeToggle.astro";
import "../styles/global.css";

export interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
const currentPath = Astro.url.pathname;

function isActive(path: string): boolean {
  if (path === "/crypto-tracker/") {
    return currentPath === "/crypto-tracker/" || currentPath === "/crypto-tracker";
  }
  return currentPath.startsWith(path);
}
---

<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <link rel="icon" type="image/svg+xml" href="/crypto-tracker/favicon.svg" />
    <link rel="icon" href="/crypto-tracker/favicon.ico" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700;800;900&family=Space+Grotesk:wght@300;400;500;700&display=swap"
    />
    <script is:inline>
      (() => {
        const key = "crypto-tracker-theme";
        let theme;
        try {
          const stored = localStorage.getItem(key);
          if (stored === "light" || stored === "dark") theme = stored;
        } catch {}
        if (!theme) {
          theme = window.matchMedia("(prefers-color-scheme: light)").matches ? "light" : "dark";
        }
        document.documentElement.dataset.theme = theme;
      })();
    </script>
    <title>{title}</title>
    {description && <meta name="description" content={description} />}
  </head>
  <body>
    <!-- Nav -->
    <nav
      style="
        background: var(--bg-nav);
        border-bottom: 1px solid var(--border);
        padding: 0.75rem 0;
        position: sticky;
        top: 0;
        z-index: 100;
        backdrop-filter: blur(8px);
        transition: background 0.3s ease, border-color 0.3s ease;
      "
    >
      <div
        class="container"
        style="display: flex; align-items: center; gap: 1.5rem"
      >
        <a
          href="/crypto-tracker/"
          style="font-weight: 700; font-size: 1.1rem; display: flex; align-items: center; gap: 0.4rem; font-family: var(--font-display)"
        >
          📈 Crypto Tracker
        </a>
        <div style="display: flex; gap: 0.75rem; font-size: 0.85rem; flex-wrap: wrap">
          <a
            href="/crypto-tracker/"
            class="nav-link"
            aria-current={isActive("/crypto-tracker/") ? "page" : undefined}
          >
            Inicio
          </a>
          <a
            href="/crypto-tracker/top"
            class="nav-link"
            aria-current={isActive("/crypto-tracker/top") ? "page" : undefined}
          >
            Top
          </a>
          <a
            href="/crypto-tracker/search"
            class="nav-link"
            aria-current={isActive("/crypto-tracker/search") ? "page" : undefined}
          >
            Buscar
          </a>
          <a
            href="/crypto-tracker/favorites"
            class="nav-link"
            aria-current={isActive("/crypto-tracker/favorites") ? "page" : undefined}
          >
            ⭐ Favoritos
          </a>
        </div>
        <div style="margin-left: auto">
          <ThemeToggle />
        </div>
      </div>
    </nav>

    <!-- Page content -->
    <main>
      <slot />
    </main>

    <!-- Footer -->
    <footer
      style="
        text-align: center;
        padding: 2rem 1rem;
        color: var(--text-muted);
        font-size: 0.8rem;
        border-top: 1px solid var(--border);
        margin-top: 3rem;
      "
    >
      Datos: CoinGecko &middot; Crypto Tracker &copy; 2026
    </footer>
  </body>
</html>
